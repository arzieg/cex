Erzeuge die PF4SH neu

Abfragen der notwendigen Informationen

JSON Library: https://github.com/json-c/json-c

Funktionen:
    add host | sid |  (tag, clustername, role, hostname, ...)
    delete  (tag, clustername,role, hostname, ...)
    remove  
    update 
    list clustename | host| storage | SID | 
    load config
    save config
    save config as 
    validate config <system>
    

interaktiv
1. Abfrage Szenario
    Single DC oder Replikation? 
    SO oder SU
    wenn SU, dann Single SID oder MCOS
    Toolserver
    ISCSI Server

typedef struct system

building blocks: 
0. main (pf)
1. allgemeine Hilfsfunktionen (helpfkt)
2. interaktiv
3. batch
4. SID Management (sidmgmt)
5. Server Management (srvmgmt)
6. allg. pf pflege


#
check mac-address auf Richtigkeit
    CustomString *custom_getline(FILE *stream, int minchars, int maxchars,char *stringfunction) 
      Hier wäre der Check von CustomString_check_regex(new, stringfunction) zu erweitern.
      Regex alleine Reicht nicht, es kann ja auch sein, dass nur ein Teil gefunden wird. Bsp. 
       Hostname = abcdefg-000*1, wenn regex nun Buchstaben, Nummern, Hyphen erlaubt, dann ist 1 ein Ergebnis
       In diesem Fall unterscheidet sich die Anzahl der Zeichen der Eingabe mit der Anzahl der Zeichen des 
       Suchergebnis uns sollte ein Fehler zurückgeben. 

bei den Netzwerkinformationen nur eine Iteration durchlaufen und bei hostnamen dann die Anzahl nehmen





